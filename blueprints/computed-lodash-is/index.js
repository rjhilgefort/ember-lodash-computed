/**
 * EX: `ember generate computed-lodash-is foo`
 */
module.exports = {
  description: 'Generates an ember-lodash-computed computed for `is` method.',

  locals: function(options) {
    return options;
  }
};
